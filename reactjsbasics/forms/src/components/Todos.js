import React from 'react'

const Todos = (props) => {
    const [todos] = props.todos
    const todo=todos.map((todo,index)=>{
        <p> </p>
})
  return (
    <div>
      
    </div>
  )
}

export default Todos